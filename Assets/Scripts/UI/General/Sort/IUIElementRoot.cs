namespace UI.General.Sort
{
    /// <summary>
    /// Marker interface used to identify a logical root UI element.
    /// Serves as a tag for grouping UI elements under a common root,
    /// e.g. for analytics, tracking, or UI hierarchy identification.
    /// </summary>
    public interface IUIElementRoot
    {
    }
}